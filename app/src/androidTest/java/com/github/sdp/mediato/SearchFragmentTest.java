package com.github.sdp.mediato;

import static org.junit.Assert.assertEquals;

import android.widget.TextView;
import androidx.fragment.app.testing.FragmentScenario;
import androidx.test.ext.junit.runners.AndroidJUnit4;
import org.junit.Test;
import org.junit.runner.RunWith;

@RunWith(AndroidJUnit4.class)
public class SearchFragmentTest {

  @Test
  public void testSearchFragmentTextView() {
    /*try (FragmentScenario<SearchFragment> scenario = FragmentScenario.launchInContainer(
        SearchFragment.class)) {
      scenario.onFragment(fragment -> {
        TextView textView = fragment.getView().findViewById(R.id.text_search);
        assertEquals("Search", textView.getText().toString());
      });
    }*/
  }

}


